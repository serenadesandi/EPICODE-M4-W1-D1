//creare una funzione che controlli due numeri interi, ritorna true se uno dei due è 50 o se la somma dei due è 50//

function checkNumber(a,b) {
if (a===50 || b===50 || (a+b)===50) {
return true;
} else {
    return false;
}
}

